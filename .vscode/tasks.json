{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build SDL2 app",
            "type": "shell",
            "command": "gcc src/main.c src/utils.c -o build/myapp $(sdl2-config --cflags --libs) -lSDL2_image -lSDL2_ttf -lSDL2_mixer -lm && ./build/myapp",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
